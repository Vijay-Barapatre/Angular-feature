# 🟦 Exercise 2: Operators

**Difficulty:** Beginner | **Time:** 20 minutes

## 📋 Problem Statement
Use common RxJS operators to transform and filter streams.

## ✅ Requirements
- [ ] Use `map()` to transform values
- [ ] Use `filter()` to filter values
- [ ] Chain multiple operators with `pipe()`
