# 🟦 Exercise 3: Effects

**Difficulty:** Intermediate | **Time:** 20 minutes

## 📋 Problem Statement

Use `effect()` to perform side effects when signals change (logging, localStorage, etc).

## ✅ Requirements

- [ ] Create a theme signal ('light' | 'dark')
- [ ] Use effect() to log theme changes
- [ ] Persist theme to localStorage
- [ ] Load theme from localStorage on init
